# 口袋精灵联机复刻版架构草案

## 目标边界

第一版只解决 4 个核心问题：
- 登录与实时连接
- 世界状态同步
- 宠物养成与编队
- 服务端权威战斗与结算

第一版明确不做：
- 公会、交易行、邮件、排行榜
- 跨服与多进程拆分
- 实时 PvP
- 开放世界实时技能战斗

## 总体架构

- 客户端：Godot 4 + GDScript
- 服务端：Go 单体服 + WebSocket + HTTP
- 通信：`HTTP(S)` 登录，`WSS` 实时消息，消息体使用 `Protobuf`
- 持久化：PostgreSQL
- 短时状态：Redis

## 设计原则

- 客户端只负责表现、输入、UI 和本地轻预测
- 服务端负责最终判定、同步广播、战斗结算、防作弊
- 所有影响结果的动作都以“意图”上报，而不是客户端直接提交结果
- 世界同步和战斗同步分离，分别维护自己的状态版本号

## 服务端模块

### auth
- HTTP 登录
- JWT 签发
- `ws_token` 签发与消费

### session
- WebSocket 会话绑定
- 心跳管理
- 顶号与断线重连

### player
- 玩家角色主数据
- 当前场景、位置、等级、货币
- 编队引用与基础属性汇总
- 经验升级与属性点分配入口（委托 `progression` 模块）

### progression
- 玩家等级经验配置与属性转化率配置
- 经验结算、连升、溢出结转
- 自由属性点分配与战斗属性重算
- 后台配置读写与运行时缓存刷新

### world
- 场景管理
- AOI 广播
- 玩家移动合法性校验
- 野怪遭遇

### pet
- 宠物模板读取
- 玩家宠物实例
- 宠物成长与编队

### battle
- 战斗房间
- 回合状态机
- 行为合法性校验
- 伤害、抓宠、掉落、经验结算

### bag
- 背包与道具
- 道具增减
- 道具使用校验

### protocol
- 包头编解码
- 消息号映射
- protobuf 消息编解码
- checksum 校验

## 客户端模块

### autoload
- `App.gd`：启动初始化
- `HttpClient.gd`：HTTP 登录与刷新
- `NetClient.gd`：WSS 建连、收发、心跳、重连
- `MessageRouter.gd`：按 `cmd` 路由到业务控制器
- `GameState.gd`：全局状态快照

### feature/world
- 世界场景控制
- 输入转移动意图
- 实体表现与插值
- 点击寻路保留 AStar 可达性结果，并在逐格确认无障碍的前提下优先合并为长直线和单拐角路径；只有直线路段受阻时才使用绕障节点。

### 同场景玩家移动同步
- 客户端每次移动表现上报都携带 `MOVE_INTENT_REQ.target_pos`，服务端仅在该整数场景格变化时写入数据库，避免高频重复持久化。
- 移动期间客户端以 100ms 最小间隔附带千分之一格 `precise_pos`、四方向 `facing` 和 `moving`；服务端将表现坐标限制在整数权威格半格范围后广播，数据库仍只在整数格变化时更新。
- 服务端确认玩家仍在请求中的 `scene_id` 后持久化坐标，再向同场景其他在线玩家发送 `ENTITY_MOVE_PUSH`；其他场景不会收到该坐标。
- 玩家进入世界、切换场景和断开连接时，服务端分别发送 `ENTITY_ENTER_PUSH` / `ENTITY_LEAVE_PUSH`，客户端据此创建或移除远端角色。
- 正式登录首次加载地图和世界地图快速传送完成目标地图加载后，本地人物落点统一读取当前地图根脚本导出的 `login_and_map_teleport_spawn_position`；普通门切图则在服务端确认目标场景并完成地图加载后，按来源 `portal_id` 调用当前地图脚本的 `get_portal_spawn_scene_position()`。这些地图落点配置均不加入 HTTP/WebSocket 协议、不提交服务端，也不从服务端读取。服务端继续负责快速传送和普通门的准入、目标场景切换及内部位置持久化；断线重连继续恢复服务端持久化的 `self_pos`。人物落地后的日常移动同步保持原有多人同屏逻辑，不把地图导出变量本身作为网络配置同步。
- 玩家实体摘要携带数据库编队首只宠物的 `following_pet`；客户端为远端角色创建独立 Y-Sort 跟随节点，玩家更换编队后服务端向同场景其他玩家广播实体刷新。
- 客户端远端角色只负责插值和动画展示，不启用输入、相机或碰撞；创建与刷新时都会应用实体摘要中的服务端 `skin_id` 人物形象，重新进入世界时以服务端快照中的在线玩家列表重建。
- 远端人物直接使用服务端归一化朝向和起停状态，不再根据延迟坐标猜方向；两个 100ms 移动包之间按人物速度短时连续预测，并在新包到达后平滑纠偏，预测最多持续 180ms，断包后不会无限前进。远端插值保留连续浮点坐标，像素吸附只发生在渲染层，避免逐帧取整造成的阶梯抖动。
- 远端宠物按高精度人物轨迹重新累计 24px 路径步，转向时重置路径轴并使用与本地宠物一致的移动速度。

### 权威移动运行态（实施中）

- 多人同屏移动优化清单统一维护在 `backend/docs/multiplayer-movement-optimization-plan.md`。
- `world.MovementStateRepository` 是领域层的短时权威移动状态边界；Redis适配器通过应用装配注入，WebSocket handler 不直接依赖 Redis客户端。
- Redis玩家移动状态使用 `{key_prefix}:world:movement:player:{player_id}`，保存会话、场景代次、千分之一格定点坐标、移动序号和位置版本，TTL 为 24 小时并在更新时续期。
- 状态更新使用 Lua CAS，同时比较会话、场景代次和旧移动序号；成功更新后把玩家加入 `{key_prefix}:world:movement:dirty`，供后续 PostgreSQL批量写回 worker 消费。
- 首次进入世界会以 PostgreSQL快照初始化 Redis；同一会话重复进入不会重置移动序号，新登录会话可以替换旧状态，切图成功后按目标场景和出生点重建场景移动代次。
- 普通移动已经先通过 `world.Service` 领域预检和 Redis Lua CAS，再执行现有 PostgreSQL同步写入与同场景广播；数据库热路径将在 dirty 批量写回 worker 完成后单独移除。
- 同会话重连优先用 Redis最新场景、整数位置和千分之一格位置重新查询场景快照；缓存缺失时回退 PostgreSQL并重建 Redis状态，客户端通过可选 `self_precise_pos` 恢复高精度位置。
- 普通移动、普通门和地图快速传送共享严格递增的 Redis移动序号；跨场景成功后以目标场景版本重建状态，旧场景或重复请求不能再次执行传送。
- 世界移动速度、单包最大服务端时间跨度和非主轴容差来自 PostgreSQL `world_movement_config`，服务启动时加载到 `world.Service` 只读缓存；缺少有效配置时服务拒绝启动，不回退代码常量。
- 运营后台通过 `GET/PUT /api/admin/world/movement-config` 维护移动参数；写入要求 `world_movement:edit` 权限和操作原因，数据库更新成功后由 `world.Service` 原子替换运行时快照，新收到的移动意图立即使用新值。
- 场景矩形外边界存放在 PostgreSQL `world_scene_definition.boundary_*_milli` 字段中，使用千分之一场景格闭区间；迁移 `119_world_scene_boundaries.sql` 为现有 1~26 号场景初始化边界，并要求未来新增场景同时提供合法边界。
- 服务启动时必须通过 `world.SceneBoundaryRepository` 加载全部启用场景边界到 `world.Service` 只读缓存；缺失、重复或非法矩形会阻止服务启动，普通移动也不会在当前场景缓存缺失时绕过边界校验。
- 运营后台通过 `GET /api/admin/world/scene-boundaries` 和 `PUT /api/admin/world/scene-boundaries/{scene_id}` 维护完整矩形；接口复用 `world_movement:view/edit` 权限，更新要求操作原因，写库成功后原子替换对应场景运行时快照。
- 同场景移动以 Redis最新高精度位置为起点，根据服务端经过时间计算最大合法距离；四方向输入非法或非主轴漂移超过配置容差时拒绝，候选位置先按速度裁剪、再按当前场景矩形边界裁剪，并回传最终纠偏坐标。矩形只负责场景外围，墙体和装饰阻挡仍由后续静态通行数据负责。

### feature/battle
- 战斗 UI
- 技能、目标、切宠输入
- 战斗事件表现

### feature/pet
- 宠物列表
- 宠物详情
- 编队管理

### feature/bag
- 背包列表
- 道具使用

## 强约束

- `transport -> module -> data` 单向依赖
- `ws handler` 不直接写业务逻辑
- UI 不直接发送网络包，统一通过控制器或网络层
- 战斗结果只看服务端推送
- 世界位置最终以服务端广播为准

## 系统名称富文本契约

- 数据库配置的物品、装备、宠物模板、技能、怪物、NPC、任务等系统展示名称允许保存 Godot BBCode。
- 后台统一使用富文本编辑器录入系统展示名称；客户端使用 `RichTextLabel` 渲染服务端原文。
- 玩家名与玩家自定义宠物名始终是纯文本。即使承载节点是 `RichTextLabel`，也必须先按纯文本转义后展示。
- 业务判断、关联、搜索键和数值计算必须使用稳定 ID、code 或显式配置，不得依赖带格式的展示名称。
- 当前名称编辑器支持 `[color]`、`[b]`、`[i]`、`[u]`，不要在名称中使用图片、链接或影响移动端布局的标签。

## MVP 实施顺序

1. 登录、`ws_token`、WebSocket 会话
2. 进入世界、场景快照、AOI、移动同步
3. 宠物实例、宠物列表、编队
4. PvE 战斗状态机
5. 背包、抓宠球、药品
6. 断线重连、限流、持久化优化
